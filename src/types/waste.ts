export interface WasteItem {
  name: string;
  category: string;
  tips: string;
}

export interface Category {
  name: string;
  color: string;
  icon: string;
  description: string;
}

export interface Article {
  title: string;
  content: string;
}